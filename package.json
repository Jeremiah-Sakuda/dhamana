{
  "name": "dhamana",
  "version": "0.1.0",
  "private": true,
  "description": "Verdict — the fair-drop engine: fair ticketing where you cannot oversell a seat, resell a ticket twice, or buy unverified — enforced at commit on Amazon Aurora DSQL, at flash-drop scale.",
  "type": "module",
  "scripts": {
    "dev": "next dev -p 3939",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "tsx scripts/seed.ts",
    "race": "tsx scripts/race.ts",
    "load": "tsx scripts/load-test.ts",
    "smoke:dsql": "tsx scripts/smoke-dsql.ts",
    "db:schema": "tsx scripts/apply-schema.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/dsql-signer": "^3.1071.0",
    "next": "^16.2.9",
    "postgres": "^3.4.9",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
